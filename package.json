{
  "name": "shared-skies",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev:api": "turbo run dev --filter=shared-skies-api",
    "dev:api2": "cd apps/api && npm run dev",
    "dev:socket": "turbo run dev --filter=shared-skies-socket-server",
    "dev:web": "turbo run dev --filter=shared-skies-web"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  },
  "dependencies": {
    "socket.io-client": "^4.8.1"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
